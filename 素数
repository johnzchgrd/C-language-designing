#include<stdio.h>
int sum=0,cnt=0;
//判断是否为素数 
int prime(int m){
	int i;
	for(i=2;i<=m/2;i++)
	  if(m%i==0)
	    break;
	if(i>m/2&&m!=1)
	   return 1;
	return 0; 
}
//寻找素数
void seekprimes(int m,int n){
 int i;
	if(m>n){
		i=m;
		m=n;
		n=i;
	}
	for(i=m;i<=n;i++){
		if(prime(i)){
			  printf("%d\n",i);
			  cnt++;
			  sum+=i;
		}
		
	}
} 
//将指定区间的数组保存到s[]中 
void saveprimes(int m,int n,int*s){
 int i,k=0;
	if(m>n){
		i=m;
		m=n;
		n=i;
	}
	for(i=m;i<=n;i++){
		if(prime(i)){
		   s[k]=i;
		   k++;
		}
	}
}
main(){
	int n,m,i;
	int s[501]={0}; 
	scanf("%d%d",&n,&m);
    saveprimes(m,n,s);
     for(i=0;s[i];i++){
    	printf("%d\n",s[i]); 
	 }
	return 0; 
}
